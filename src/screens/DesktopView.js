import React from "react";

const DesktopView = () => {
  return (
    <div style={{ textAlign: "center", marginTop: "50px" }}>
      <h1>🌱 Garden 데스크톱 버전</h1>
      <p>현재 데스크톱 브라우저에서 접속 중입니다.</p>
      <p>이 서비스는 모바일에서 최적화되어 있습니다.</p>
    </div>
  );
};

export default DesktopView;
